// #include : 헤더 파일을 여기에 포함시키는 기능
#include <iostream>

// 실행 과정 : 컴파일(번역 작업) -> 빌드 -> 실행

// 시작점은 main 함수
int main() {
	// std 라는 namespace -> 이름 겹치는거 방지
	// cout : 콘솔창에 출력
	std::cout << "hi";
	return 0;
}
